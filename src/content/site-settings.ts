import type { SiteSettings } from "./types";

export const siteSettings: SiteSettings = {
  countdownTarget: "2026-10-31T00:00:00+05:30",
  featureFlags: {
    vaultUnlocked: false,
    t24hTriggerEnabled: true,
  },
};
